import { FilterplayerPipe } from './filterplayer.pipe';

describe('FilterplayerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterplayerPipe();
    expect(pipe).toBeTruthy();
  });
});
